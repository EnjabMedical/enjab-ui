{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "logo",
  "title": "Logo",
  "description": "The Enjab parent logo. Light backgrounds only. Use ONLY in the 'an Enjab product' byline, not as a tool's own brand. For a tool's mark use @enjab-ui/app-mark.",
  "files": [
    {
      "path": "src/components/enjab/logo.tsx",
      "content": "import { cn } from \"@/lib/utils\";\n\n/**\n * Enjab logo. Light backgrounds only (no reversed/white version exists).\n * Loads from the hosted URL, so it works in any repo with no local asset.\n */\nexport function Logo({ size = 32, className }: { size?: number; className?: string }) {\n  return (\n    // eslint-disable-next-line @next/next/no-img-element\n    <img\n      src=\"https://ui.enjab.ae/enjab-logo.png\"\n      alt=\"Enjab\"\n      style={{ height: size }}\n      className={cn(\"w-auto select-none\", className)}\n    />\n  );\n}\n",
      "type": "registry:ui",
      "target": "components/enjab/logo.tsx"
    }
  ],
  "type": "registry:ui"
}